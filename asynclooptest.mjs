/*eslint-disable*/
// import { Bezier } from 'bezier-js';

// const curve = new Bezier(0, 0, 0, 300, 300, 0, 300, 300);

// const LUT = curve.getLUT(18);

// console.log(LUT);

//console.log(LUT[28]);
// LUT.forEach((p) => console.log(Math.floor((p.x))));

/* const bulla = (bum) => {
    return bum.length;
}

const aaray = [{"x":7, "y":5}, {"x": 3, "y": 6}];

console.log(aaray[1].x);
*/

function bum(i) {
    i++
}

let i = 0;
bum(i);
console.log(i);

